> Note: if you need to rely on this function to set the event ID for a given note with 100% accuracy, you need to use [this method instead](/scripting/scripting-api/sampler#setmultigroupindexforeventid).

